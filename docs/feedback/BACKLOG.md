# سجل الملاحظات

مرقّمة ومصنّفة. الرقم لا يُعاد استعماله أبداً.
الحالات: `جديدة` · `قيد العمل` · `منتهية` · `مشحونة` · `مؤجَّلة` · `ملغاة`

**آخر رقم مستعمل: م-١٠** · تاريخ التحديث: ٢٨ أغسطس ٢٠٢٦ · الإصدار الحالي: v0.9.7

---

## مفتوحة

### م-٣ · خلل · ملف الموازنة لا يُحفظ عند رفعه منفرداً
**الحالة:** جديدة · **الأولوية: عالية** — الملف سليم ويُرفض

> «i added this file but it didnt update the numbers»
> (`تقرير انحراف الموازنه التقديرية شهر يوليو 2026 م.xlsx`)

**السبب — مُثبَت بالتشغيل:** مساران مختلفان يصنّفان نفس الملف تصنيفين مختلفين.

| الدالة | النتيجة | المسار |
|---|---|---|
| `_classify_xlsx()` (خادم) | `budget_deviation` ✅ | مسح المجلد |
| `detectSource()` (`apps/desktop/src/main/index.ts:283`) | `suppliers_excel` ❌ | اختيار ملف مفرد |

منتقي الملف المفرد يصنّف **بالامتداد وحده**، فكل `.xlsx` يصير «ملف موردين»،
فيُمرَّر تقرير الموازنة إلى محلّل الموردين ويفشل. والفحص بقراءة أسماء الأوراق
(`'انحراف' in sheetnames`) موجود وصحيح — لكنه يعمل في مسار `batch_import` فقط.

**عطبٌ ثانٍ خلفه:** في `apps/renderer/src/pages/Import.tsx:445` تُبنى قائمة
`ordered` من `suppliers_excel` ومصادر الكشوفات فقط — فحتى لو صُنّف الملف
`budget_deviation` صحيحاً، **يسقط من الطابور بصمت**. لا بد من إصلاح الاثنين معاً.

**الإصلاح:** المنتقي يسأل الخادم عن التصنيف بدل التخمين بالامتداد (نقطة واحدة
للحقيقة)، و`ordered` تقبل `budget_deviation`.

---

### م-٤ · نقص · تأخر المقاولين
**الحالة:** مؤجَّلة — **محجوبة عليك**

حركات المقاول قيود مدين/دائن **بلا تاريخ استحقاق**، بخلاف فواتير الموردين.
لا يوجد ما يُقاس منه التأخر، ولن يُخترع رقم.

**ما يفتحها:** إرسال ٣–٤ **مستخلصات حقيقية** (Excel وPDF، من مقاولين مختلفين).
المستخلص له تاريخ اعتماد واستحقاق — منه يُبنى تأخر حقيقي.

---

### م-٥ · نقص · محلّل المستخلصات
**الحالة:** مؤجَّلة — **محجوبة عليك** (نفس الملفات المطلوبة في م-٤)

اليوم تُدخل قيمة المستخلص والاستقطاع والضمان يدوياً. الهدف: تُقرأ كما يُقرأ كشف
الحساب. لم يُبنَ لأن أي بناء بلا ملف حقيقي تخمين.

---

### م-٦ · نقص · ضبط الذكاء الاصطناعي على جهاز ويندوز
**الحالة:** مؤجَّلة — **إجراء منك**

الإعدادات ← العنوان `https://ollama.com/v1` · النموذج **`gemma4:31b`**
(قياس على ملفاتك الحقيقية: ٤/٤ صحيحة، ٥.٥ ثانية، ٨٨٥ رمزاً — الأدق والأرخص معاً).

لم يُجرَّب عندك قط. المحلّل العادي يكفي لكل كشوفك الحالية؛ الذكاء إضافة لا أساس.

---

### م-٧ · تحسين · مراجعة تصميم شاملة
**الحالة:** جديدة

مراجعة قراءة-فقط لكل الشاشات: الالتزام بنظام التصميم، أرقام قد تُضلِّل، سلامة
RTL، حالات فارغة/خطأ ناقصة، الانكسار عند ١١٠٠px وفي الوضع الداكن.
تُرتَّب بأثرها عليك — **رقم مضلِّل يسبق أي انحراف في المسافات**.

---

### م-٨ · تحسين · `ROADMAP.md` متقادم
**الحالة:** جديدة

آخر تحديث ٨ أغسطس ويصف v0.2، والمشروع في v0.9.7 بعد ٣٠ إصداراً.
خارطة لا تصف المشروع أسوأ من غياب خارطة: تُقرأ فيُبنى عليها قرار خاطئ.

---

## منتهية — بانتظار الشحن

### م-٩ · نقص · نسخة احتياطية تلقائية قبل كل تحديث
**الحالة:** منتهية · تُشحن في v0.10.0

تُؤخذ نسخة من قاعدة البيانات لحظة **اكتمال تنزيل** التحديث، لا لحظة التثبيت —
لأن للتثبيت ثلاث طرق (زر «تحديث الآن»، زر الإعدادات، و`autoInstallOnAppQuit`
الصامت عند الإغلاق)، واكتمال التنزيل نقطة واحدة تغطّيها جميعاً.
تُحفظ في `backups/before-update-<الإصدار>-<الطابع>.db`، ويُبقى على آخر عشر نسخ.

فشل النسخ **لا يوقف التحديث** — تعطيل تحديثٍ سليم بسبب تعذُّر النسخ ضرره أكبر.

---

## مغلقة

| # | الملاحظة | النتيجة |
|---|---|---|
| **م-١** | تعدد المشاريع للموردين والمقاولين | ✅ شُحنت في **v0.8.0** |
| **م-٢** | ملف «تداين اليرموك» لا يُحدِّث الأرقام | ✅ أُصلح في **v0.9.5** — تحقّقتُ بالتشغيل: يُحفظ الآن ويُنشئ الحساب `2110124` تلقائياً بمدة «غير محدّدة». الفشل كان على إصدار أقدم. |
| **م-١٠** | تقرير تحليلي مختصر: أختار المشروع فيعرض الإجمالي لكل شركة لا لكل سند | ✅ شُحن — `format: 'projectSummary'` في `Report.tsx` |
