# EGCO Dashboard — قواعد العمل

لوحة مالية عربية RTL (Electron + React/TS + FastAPI/SQLite) لشركة إعمار الخليج
المصرية للمقاولات. محلية بالكامل: لا خادم، لا سحابة، البيانات في SQLite على الجهاز.

---

## ١ · الملاحظات — الالتقاط قبل التنفيذ

المستخدم يعطي ملاحظاته **على دفعات أثناء عمل جارٍ**، لا مرة واحدة. هذا طبيعي:
يكتشفها وهو يستعمل التطبيق فعلاً.

**القاعدة:** أي ملاحظة تصل أثناء العمل تُكتب فوراً في `docs/feedback/INBOX.md`
بنصّها كما قيلت، **ثم يُكمَل العمل الجاري**. لا يُقاطَع عمل جارٍ إلا لخللٍ في رقم
معروض — لأن قرارات المستخدم المالية تُبنى عليه.

تجاهل هذه القاعدة يُنتج أربعة أشياء نصف مبنية بدل واحد منتهٍ. حدث فعلاً.

النظام كاملاً في `docs/feedback/README.md`. الترقيم `م-N` لا يُعاد استعماله.

---

## ٢ · الأرقام

- **`decimal.Decimal` دائماً** عبر `D()` / `money()` في `app/domain/payables.py`.
  التقريب **مرة واحدة عند الحدّ**. جمع أرقام مقرَّبة سلفاً يُنتج انحرافاً بالهللات
  يجعل شاشتين تتناقضان — حدث ثلاث مرات.
- **الترتيب والتصفية على الخادم** على المجموعة المصفّاة كاملةً، لا في المتصفح.
  ترتيب صفحةٍ في المتصفح يجعل سطر الإجماليات يصف مجموعةً أخرى.
- **كل رقم قابل للتتبّع** إلى أجزائه. رقمٌ مغلق لا يُفسَّر ليس مقبولاً هنا.
- **لا تخترع رقماً أبداً.** إن لم تدعم البيانات الحساب، قُل ذلك. عمود فارغ صادق
  خيرٌ من رقم مفبرك (مثال: تأخر المقاولين — حركاتهم بلا تاريخ استحقاق).
- **اصطلاح إشارة الكشف:** المطبوع سالباً = نحن ندين لهم · موجباً = يدينون لنا.
  مُتحقَّق منه على ١٨ كشفاً حقيقياً. لا يُعكَس.
- **بادئة الحساب تحسم النوع:** ٢١١ مورد · ٢١٢ مقاول · ٢١٦ ضمان. غير ذلك: اسأل.

## ٣ · الذكاء الاصطناعي

يقرأ ويكتب **نصاً فقط**. كل رقم يأتي من محرك Decimal المختبَر. لا يحسب شيئاً.

## ٤ · اللغة والواجهة

كل نص للمستخدم بالعربية · كل المبالغ بالريال (ر.س) · الشريط الجانبي **يمين**.
لا لون أو مسافة خارج `tokens.css`.
**فخ موثَّق:** `th, td { padding: 10px 12px }` خارج السلّم عمداً ليضبط ارتفاع
الصف ٤٤px — لا يُصحَّح.

## ٥ · الخادم

Python **3.9** — لا `X | None` وقت التشغيل، استعمل `Optional`/`List`.
حذف منطقي (`deleted_at`) في كل مكان.

## ٦ · بوابة الإغلاق

لا تُغلق ملاحظة ولا يُشحن إصدار قبل:

1. `cd services/api && ./.venv/bin/python -m pytest tests/ -q` — الكل ناجح
2. `cd apps/renderer && npx tsc --noEmit` — صامت
3. **تحقُّق على بيانات حقيقية**، لا على مثال مصنوع
4. سطر في `SHIPPED.md` يقول **كيف** تحقّقنا

**لا تُعلن نجاحاً لم تُشغِّله.** الوكلاء أبلغوا عن عمل لم يحدث؛ تحقّق بنفسك دائماً.

## ٧ · الوكلاء المتوازون

**ملكية ملفات صارمة** — لا يلمس وكيلان ملفاً واحداً أبداً. حدث تعارض كاد يمحو
عملاً سليماً. وكيل المراجعة **قراءة فقط**.

## ٨ · الشحن

`git tag vX.Y.Z` → بناء تلقائي (GitHub Actions) → **نشر مباشر** يصل جهاز المستخدم
عند الفتح التالي. لا خطوة مراجعة بشرية بينهما — لذلك البوابة أعلاه ليست اختيارية.

**نسخة احتياطية تلقائية** من قاعدة البيانات تُؤخذ عند اكتمال تنزيل التحديث
(`backups/before-update-*.db`، آخر عشر نسخ).

رسالة الوسم تذكر الأرقام المغلقة صراحةً: «أغلق م-٤١، م-٤٤، م-٤٧».

## ٩ · الأمان

من يملك صلاحية الدفع للريبو يملك جهاز المستخدم عملياً — النشر تلقائي بلا مراجعة.
لا يُضاف Collaborator بلا ثقة كاملة. لا تُطبع مفاتيح أو رموز في السجلات أبداً.
